//operador ternario
const edad = 18;

const mayorEdad = (edad >= 18) ? ("Es mayor de edad") : ("No es Mayor de edad");

console.log(mayorEdad);


const myConst = true;
const OtherConst = myConst || "valor por default";
console.log(OtherConst);